<%= form_with(model: special_device_list, local: true) do |form| %>
  <% if special_device_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(special_device_list.errors.count, "error") %> prohibited this special_device_list from being saved:</h2>

      <ul>
        <% special_device_list.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
